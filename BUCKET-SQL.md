-- تنبيه: SQL Editor لا يمكنه إنشاء Bucket (التخزين منفصل عن قاعدة البيانات)
-- هذا الملف للتوضيح فقط. لإنشاء البكت، استخدم السكربت أدناه.

SELECT 'لا يمكن إنشاء storage bucket من SQL Editor. استخدم Storage → New Bucket أو شغّل create-bucket.js';
